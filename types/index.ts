export interface User {
  id: string;
  email: string;
  name: string;
}

export interface Article {
  id: string;
  title: string;
  summary: string;
  published_date: string;
  topic: string;
  link: string;
}